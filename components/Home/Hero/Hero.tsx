import HeroSlider from "./HeroSlider";

const Hero = () => {
  return (
    <div className="h-[70vh] w-screen overflow-x-hidden sm:h-screen">
      <HeroSlider />
    </div>
  );
};

export default Hero;
